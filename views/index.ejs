<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Engineers List</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>
<body>
  <h1>Senior Engineers 👨‍💻</h1>
  <form action="/engineer" method="POST" class="buttons">
    <button type="submit" value="Feisal" name="choice">🍗</button>
    <button type="submit" value="Osama" name="choice">🍗</button>
    <button type="submit" value="Aar" name="choice">🍗</button>
  </form>
  <div id="engineerContainer" class="hidden">
    <%if (locals.engineer) {%>

      <h2 id="engineerTitle">
        <%= engineer.name %>
      </h2>

      <h3>Experience:</h3>
      <ul id="experiencesList">
        <li>
          <%= engineer.experience.frontend.work %>, <%= engineer.experience.frontend.position %>
        </li>
        <li>
          <%= engineer.experience.backend.work  %>

        </li>
        <% engineer.experience.agencies.forEach(agency=> { %>
          <li>
            <%= agency.projects %> of <%= agency.names %>
          </li>
          <% }) %>
      </ul>

      <%} else {%>
        <h2>Pick your favourite Engineer in the list 👆</h2>
        <%}%>
  </div>
</body>
</html>